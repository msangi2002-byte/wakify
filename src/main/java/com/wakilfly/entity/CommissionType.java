package com.wakilfly.entity;

public enum CommissionType {
    ACTIVATION, // Commission for activating a new business
    BUSINESS_ACTIVATION, // Same as ACTIVATION (alias)
    SUBSCRIPTION_RENEWAL, // Commission for subscription renewals
    REFERRAL // Referral bonus
}
